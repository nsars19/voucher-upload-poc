export default function SigningIn() {
  return (
    <span
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
      className="App-header"
    >
      <span
        style={{
          color: "lightblue",
        }}
      >
        Logging in
      </span>
    </span>
  );
}
