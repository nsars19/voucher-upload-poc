export default function SigningIn() {
  return (
    <span
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
      className="App-header"
    >
      <span
        style={{
          color: "#61dafb",
          fontSize: "2em",
        }}
      >
        Logging in
      </span>
    </span>
  );
}
